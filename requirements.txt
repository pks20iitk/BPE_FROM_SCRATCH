# Core (no external deps required — stdlib only)
# Optional: for faster regex with Unicode property escapes (\p{L}, \p{N})
regex>=2023.0.0

# Optional: official SentencePiece library (for SentencePieceWrapper)
# sentencepiece>=0.1.99

# Optional: OpenAI tiktoken (for GPT-2/4 tokenizer comparison)
# tiktoken>=0.5.0

# Testing
pytest>=7.0.0
